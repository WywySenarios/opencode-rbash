/**
 * Test setup — with vitest's `globals: true` in vitest.config.ts,
 * lifecycle hooks (beforeAll, afterAll, etc.) are available globally.
 * This file is kept as a placeholder for future vitest setup if needed.
 */
